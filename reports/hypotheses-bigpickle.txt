## 2026-09-05 17:37:46 UTC [target] (model bigpickle)
[PRIO] www.obi.de/account/api/public/jwt/validate,7.1, b=10 t=9 g=3 c=5 f=8
[PRIO] api.obi.com/.../seller-data-hub-service/api/v1/public,6.15, a=5 b=6 t=4 g=10 c=4 f=9
[PRIO] api.obi.com/.../{transaction|order|invoice}-service (mule-realm),5.95, a=7 b=9 t=5 g=2 c=3 f=6
[HYP] JWT Validation Endpoint — Algorithm Confusion / Session Boundary Probe
class: AUTH
asset: www.obi.de/account/api/public/jwt/validate
confidence: 70
reasoning: Endpoint live with browser UA (GET/HEAD 200 text/javascript len 0, clears obi-auth; POST unsessioned 405; Accept: application/json → 406 content-negotiation). Regi/auth bundle referenced in live homepage DOM (/regi/auth/assets/regi-hey-obi-login-de_de-inc.html-*.js) confirms JWT issue/validate flow. Alg confusion (RS256→HS256/none) is testable only with a session JWT.
evidence_needed: Authenticated obi-auth JWT; baseline valid POST → 200; crafted alg:none POST → 401/400 (safe) vs 200 (vuln).
verify_steps: Later (auth): POST valid JWT → baseline; POST alg:none artifact → 401/400 (safe) vs 200 (vuln).
impact: ATO 10M+ heyOBI accounts incl. payment/purchase history. Severity: CRITICAL.
testability: AUTH_HELPED
[HYP] Seller Data-Hub Public OAS Spec — Close Enumeration Tree for Unmapped /public/ Handlers
class: MISCONFIG
asset: api.obi.com/trx-api/fulfillmentsellersteering/seller-data-hub-service/api/v1/public
confidence: 75
reasoning: MuleSoft portal HTML (CORS:*) embeds S3 signed download URLs for all marketplace API specs. seller-data-hub-service spec not yet extracted; current enumeration found exactly 3 handlers (feature-toggle, seller-side-panel/{id}, imprint-documents/{key}/{cp|gtc}) via 35-name brute force. OAS spec will definitively close the public handler tree and reveal any unmapped endpoints (e.g., upload tokens, seller postings).
evidence_needed: S3 signed URL for seller-data-hub-service OAS/RAML spec from portal HTML; grep spec for /public/* paths.
verify_steps: GET https://api.obi.com/ (browser UA) → extract S3 signed URL for seller-data-hub-service spec → download → grep for /public/ handlers.
impact: Completes attack surface map; may reveal higher-value unauth endpoints. Severity: INFORMATIONAL→MEDIUM depending on findings.
testability: PASSIVE
[HYP] Cross-Seller IDOR via Enumerated trxId Namespace on Mule-Realm Services
class: IDOR
asset: api.obi.com/trx-api/fulfillmentsellersteering/{transaction|order|invoice}-service
confidence: 55
reasoning: Registry proof shows dense sequential trxIds (1, 100000+) are master seller keys; gated services authorize via mule-realm scope credential, not per-object ownership; portal OAS declares unscoped GETs (/v1/transactions/{id}). Enumeration now gives the exact attacker-controlled ID space.
evidence_needed: Valid seller clientId/clientSecret.
verify_steps: Later (creds): GET /v1/transactions/{other-trxId} → 403 (safe) vs 200/leak (vuln).
impact: Cross-tenant order/invoice/financial extraction across sellers. Severity: HIGH (unproven).
testability: AUTH_HELPED
[FINAL] 1. JWT Validation alg-confusion (www.obi.de, 70, AUTH_HELPED, CRITICAL) — unchanged, awaiting session JWT
[FINAL] 2. Seller Data-Hub OAS Spec Extraction (api.obi.com, 75, PASSIVE, INFORMATIONAL→MEDIUM) — new, closeable this cycle
[FINAL] 3. Cross-Seller IDOR (api.obi.com, 55, AUTH_HELPED, HIGH) — extended to seller-data-hub service
[PARKED] Baqend BaaS Data Exposure: DOM-confirmed (install.js loaded, obi-de.app.baqend.com) but data class unknown and private; no new payload path; confidence remains 40 — park.
[PARKED] Internal Recommendations v6 500-variant: negative result invariant to input; dead end (confidence 25).
[NEXT] PROBE: GET https://api.obi.com/ with User-Agent: Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/125.0.0.0 Safari/537.36 → extract S3 signed download URL for **seller-data-hub-service** API spec from embedded JSON → download spec → grep for all `/api/v1/public/*` path handlers to definitively close the unauthenticated enumeration tree.
[LEARN] ACCEPTED MISCONFIG @ api.obi.com: seller-data-hub-service/api/v1/public/ is a full unauthenticated seller registry — /public/{cc}/seller-side-panel/{trxId} returns complete imprint+settings for sequential IDs 1, 100000–100550+ across DE/AT/IT/PL; numeric vs alpha inputs disclose trxId↔vtexSellerId dual moniker semantics; /public/feature-toggle identical across 6 countries; siblings are 401 mule-realm — gate divergence proven as registry enumeration.
[LEARN] ACCEPTED ENDPOINT-MAP @ api.obi.com: /public tree is exactly feature-toggle + seller-side-panel/{id} + imprint-documents/{key}/{cp|gtc} (35 candidate names 404); imprint-documents key=obiecomprod → PDF (Widerrufsbelehrung/AGB), traversal `ct%2F..` → 403 blocked.
[LEARN] ACCEPTED AUTH @ api.obi.com: seller bundle fetches seller-data-hub /public/ with mode:cors + credentials:include from assets.obi.de origin; base api.obi.com is CORS:* — cross-origin session-bearing calls are part of the portal design (no validated flaw yet).
[RISK] obi: 42/100 — ≈150 GETs at ≤1 rps, read-only, browser UA, against a service the OBI app itself calls unauthenticated; responses were seller imprint/business data (legally-required-public company imprint incl. director names/VATIN aggregations) — no customer PII, no financial data, no auth tokens, no auth-bypass mutation; sequence sweep may approach enumeration-volume thresholds but stays within passive bounds. Net: one confirmed registry-level finding (MEDIUM) + trxId namespace recovered for gated-service targeting.
